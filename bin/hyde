#!/usr/bin/env ruby
require './lib/structure'

args = ARGV
case args[0]
when "new"
  Structure.new(args[1])
when "build"
  raise ArgumentError.new("Path does not exist") if !File.directory?(Dir.home + args[1])
end
#create skeleton



# ##############
# Your code here
# ##############
